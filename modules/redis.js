// sudo npm install --save redis
// brew install redis
/*
const redis = require('redis');

const redisClient = redis.createClient(process.env.REDIS_PORT);

console.log('redis ::::: ' + redisClient);
module.exports = redisClient
*/